#include "main.h"
/**
 * mul - multiplies two integers.
 * @a: first integer for argument.
 * @b: second integer for argument.
 * Return: Always 0 (Success)
 */
int mul(int a, int b)
{
	return (a * b);
}
