#include <stdio.h>
/**
 * main - the use of function printf to print
 *
 * Return: Always o (Success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
